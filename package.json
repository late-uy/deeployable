{
  "name": "deeployable",
  "private": true,
  "version": "0.0.0",
  "workspaces": [
    "api",
    "ui"
  ],
  "scripts": {
    "build": "npm run build -w api && npm run build -w ui",
    "start": "npm run start -w api & npm run start -w ui",
    "dev": "npm run dev -w api & npm run dev -w ui",
    "postinstall": "npm run generate -w api || true"
  }
}


